#ifndef __MHm10__
#define __MHm10__

class MHm10
{
public:
    MHm10 (void);
    ~MHm10 (void);

	boolean Initialize (void);
	int Command(char* vCommand, char* vParam);

private:
	static MHm10 *s_pThis;
	static char statusHm10Init;

    unsigned char pBuffer[255];
};

#endif
